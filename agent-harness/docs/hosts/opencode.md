# Installing ruvdrone into OpenCode

1. `opencode auth login` to set a model provider.
2. `cd` here and run `opencode` — the TUI reads `.opencode/opencode.json`.
3. Inside the TUI run `/mcp` to verify `ruvdrone` is registered.
